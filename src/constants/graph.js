export const WIDTH_GRAPH = 800;
export const HEIGHT_GRAPH = 800;

export const WIDTH_PADDING = 60;
export const HEIGHT_PADDING = 60;

export const SIZE_X = WIDTH_GRAPH - 2 * WIDTH_PADDING;
export const SIZE_Y = HEIGHT_GRAPH - 2 * HEIGHT_PADDING;

export const DEFAULT_MIN_X = 0;
export const DEFAULT_MIN_Y = 0;

export const DEFAULT_STEP = 0.0001;
export const MAX_SCALE_Y = 10;

export const SEGMENTS = 10;
